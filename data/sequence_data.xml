<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- the code are something that represents the sequence-->
    <!-- this is for create a record in the sequence model to generate the sequence-->
    <!-- -->
    <record id="seq_hospital_patient" model="ir.sequence">
        <field name="name">Hospital Patient</field>
        <field name="code">hospital.patient</field>
        <field name="prefix">HP</field>
        <!-- the padding is the total length -->
        <field name="padding">5</field>
        <field name="company_id" eval="False"/>
    </record>

</odoo>